#include <stdio.h>

/**
 * main - Prints all single digit numbers of base 10 starting from 0.
 *
 * Return: - Always 0.
 */
int main(void)
{
int bases;

for (bases = 0; bases < 10; bases++)
printf("%d", bases);

printf("\n");

return (0);
}
