#include "main.h"

/**
 * _abs: Computes the absolute value of an integer.
 * @n: The integer to be computed.
 *
 * Return: the absolute value.
 */

int _abs(int x)
{

if (x >= 0)
return (x);
else
return (-x);
}
