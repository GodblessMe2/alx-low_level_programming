#include "main.h"

/**
 * abs: Computes the absolute value of an integer.
 * @x: The integer to be computed.
 *
 * Return: the absolute value.
 */

int _abs(int x)
{

if (x >= 0)
return (x);
else
return (-x);
}
