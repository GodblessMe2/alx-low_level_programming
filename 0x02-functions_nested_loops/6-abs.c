#include "main.h"

/**
 * _abs: Computes the absolute value of an integer.
 * @n: The integer to be computed.
 *
 * Return: the absolute value.
 */

int _abs(int x)
{
int n; 
if (n >= 0)
return (n);
else
return (-n);
}
