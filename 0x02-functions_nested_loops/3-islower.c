#include "main.h"

/**
 * islower - checks for lowercase character.
 *
 * Return: 1 if the character is lower or 0.
 */

int islower(int c)
{
if (c <= 'z'; && c >= 'a')
return (1);
else
return (0);
}

